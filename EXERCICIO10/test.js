let num = [ 2, 3, 4, 5 , 9]

    for (c = 0 ; c < num.length ; c++){
        console.log (`A posição ${c} do seu awway é ${num[c]}`)
    }

console.log ("Agora da melhor forma para um array:")

    for ( d in num ){
        console.log (`A posição ${c} do seu awway é ${num[d]}`)
    }
console.log (num.indexOf(4))
var b = num.indexOf(4)
console.log (`O valor 4 esta na posição ${b}`)